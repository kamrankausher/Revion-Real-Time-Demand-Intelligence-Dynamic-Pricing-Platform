"""Package setup for editable installation."""

from setuptools import setup, find_packages

setup(
    name="demand-intelligence-platform",
    version="1.0.0",
    description="Real-Time ⚡ Revion Platform",
    packages=find_packages(),
    python_requires=">=3.10",
)
